# Removing All Dummy Data

Before running the frontend, you need to install dependencies:

```bash
npm install
```

This will install all required packages including Vite.

After that, I'll remove all dummy/mock data from the pages.
